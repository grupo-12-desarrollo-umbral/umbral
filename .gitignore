coverage-report/
coverage/
.dotnet-cli/
.nuget/
/appsettings.Development.json

# Test-runner caches (vitest, playwright)
node_modules/
test-results/

# Ephemeral agent handoff scratch — written per worktree by the handoff skill,
# never committed (see backend/.agents/skills/handoff/SKILL.md)
/HANDOFF.md

# Git worktrees
/umbral-hu-*
/umbral-des-*
